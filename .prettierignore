node_modules/
dist/
.wrangler/

# Generated by `wrangler types` — half a megabyte of ambient declarations.
worker-configuration.d.ts

# Recorded VCR cassettes. Committed, but written back by the recorder rather
# than hand-edited, so formatting them makes `npm run check` fail after every
# re-record.
test/snapshots/
